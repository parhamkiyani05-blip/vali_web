import { useEffect,useState } from 'react'; import { request } from '../lib/api';
export default function Users(){const [items,setItems]=useState([]);const [f,setF]=useState({username:'',fullName:'',role:'employee',password:''});const load=()=>request('/api/users').then(setItems);useEffect(()=>{load()},[]);async function add(e){e.preventDefault();await request('/api/users',{method:'POST',body:JSON.stringify(f)});setF({username:'',fullName:'',role:'employee',password:''});load()}async function toggle(x){await request(`/api/users/${x.id}`,{method:'PATCH',body:JSON.stringify({active:!x.active})});load()}
return <section><div className="section-head"><div><h2>کارمندان</h2><p>مدیر می‌تواند کاربر بسازد، فعال/غیرفعال کند و بعداً ویرایش کامل انجام دهد.</p></div></div><div className="panel"><form className="grid-form" onSubmit={add}><label>نام کاربری<input required value={f.username} onChange={e=>setF({...f,username:e.target.value})}/></label><label>نام کارمند<input required value={f.fullName} onChange={e=>setF({...f,fullName:e.target.value})}/></label><label>نقش<select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="manager">مدیر</option><option value="office">دفتر‌دار</option><option value="employee">کارمند</option></select></label><label>رمز عبور<input required type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><button>ساخت حساب</button></form></div><div className="panel"><table><thead><tr><th>نام</th><th>نام کاربری</th><th>نقش</th><th>وضعیت</th><th/></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.full_name}</td><td>{x.username}</td><td>{x.role}</td><td>{x.active?'فعال':'غیرفعال'}</td><td><button className="ghost" onClick={()=>toggle(x)}>{x.active?'غیرفعال':'فعال'}</button></td></tr>)}</tbody></table></div></section>}
