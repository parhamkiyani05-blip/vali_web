import { useEffect,useState } from 'react'; import { getSession,request } from '../lib/api';
export default function Drivers(){ const role=getSession()?.user?.role; const [items,setItems]=useState([]); const [f,setF]=useState({name:'',truckNumber:'',phone:'',language:'fa'}); const load=()=>request('/api/drivers').then(setItems); useEffect(()=>{load()},[]); async function add(e){e.preventDefault();await request('/api/drivers',{method:'POST',body:JSON.stringify(f)});setF({name:'',truckNumber:'',phone:'',language:'fa'});load()} async function del(id){if(confirm('به بایگانی منتقل شود؟')){await request(`/api/drivers/${id}`,{method:'DELETE'});load()}}
return <section><div className="section-head"><div><h2>راننده‌ها</h2><p>نام، شماره کامیون، تماس و زبان پیش‌فرض فاکتور.</p></div></div><div className="panel"><form className="grid-form" onSubmit={add}><label>نام راننده<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>شماره کامیون<input required value={f.truckNumber} onChange={e=>setF({...f,truckNumber:e.target.value})}/></label><label>شماره تماس<input required value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>زبان<select value={f.language} onChange={e=>setF({...f,language:e.target.value})}><option value="fa">فارسی</option><option value="tr">Türkçe</option></select></label><button>افزودن راننده</button></form></div><div className="panel"><table><thead><tr><th>راننده</th><th>کامیون</th><th>تماس</th><th>زبان</th><th/></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.truck_number}</td><td>{x.phone}</td><td>{x.language==='tr'?'Türkçe':'فارسی'}</td><td>{role==='manager'&&<button className="ghost danger" onClick={()=>del(x.id)}>بایگانی</button>}</td></tr>)}</tbody></table></div></section> }
